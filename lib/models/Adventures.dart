class AdventureActivities {
  String activities, places, location, activityPlace;
  String distance, globalLocation;
  String about, departures, meetingPoint, guide;

  AdventureActivities({this.activities});

  void getActivityType(String activityName) {
    if (activityName == 'kayaking'.toUpperCase()) {}
  }
}
